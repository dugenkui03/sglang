Use case: infographic-diagram
Asset type: 插入 SGLang 超参数调优中文文档的 GPU 显存科普图。
Primary request: 一张没有主标题、没有副标题、没有页眉的横向 4:3 科普信息图。直接从 GPU 显存分区示意开始，清楚说明模型权重、KV 缓存池、执行所需显存的关系，以及调参时为什么要平衡。
Style/medium: 专业工程科普，清晰中文无衬线字体，简洁显存分区条、请求图标和从左到右的箭头，留白充分，清晰可读。高分辨率。
Background: 整张图必须完全不透明，纯白色画布，所有像素 alpha=255。不要透明背景、黑色背景、抠图或纹理。无 logo、无水印。
Composition: 画布顶部直接放分区图，没有任何大标题。三个横向区域从左到右分成模型权重、KV 缓存池、执行所需显存；下方放两种分配情况；底部只放概念说明和调优原则。所有文字在边界内，留白充分。

必须准确表达并尽量逐字使用以下文字：
顶部显存条的小标签：“GPU 显存（示意，非固定比例）”
三个区块：
“模型权重”“同一模型配置下相对固定”
“KV 缓存池”“保存上下文，支撑并发请求”
“执行所需显存”“激活值 + CUDA 图缓冲区”
模型权重和 KV 缓存池上方用一条括号覆盖这两个区块，标签：“--mem-fraction-static 控制这两部分的比例”。括号不能覆盖第三个区块。
KV 缓存池下面的两个小框：“正在推理的请求所需 KV”“可复用的历史前缀 KV”。都由 KV 区块连接，不画成每个请求有独占的一份缓存。
执行区块下面的一个小框：“满足当前批次计算的显存需求”。

中部两个对照行，权重区块宽度在两行完全相同，整条总宽度相同：
第一行小标签：“扩大 KV 缓存池”
分区条：相对固定的权重区块、较大的 KV 区块、较小的执行区块。
右侧解释：“可容纳更多请求或更长上下文”“执行余量减少，峰值不足会 OOM”
第二行小标签：“增加执行显存余量”
分区条：相对固定的权重区块、较小的 KV 区块、较大的执行区块。
右侧解释：“更容易满足批次计算需求”“KV 容量减少，并发可能受限”
不要把扩大 KV 池画成并发降低；不要把执行预留显存多画成并发自动提高。仅用示意条宽度，不给出 GB 数值、百分比或性能数字。

底部三个紧凑说明框，作为图解正文而非标题：
“并发量受 KV 容量与执行显存共同限制”
“token usage：KV 缓存池使用率\n缓存命中率：历史计算复用程度”
底部重点说明原样使用这句话，可分成两行：“在执行不 OOM 的前提下，尽量扩大可用 KV 容量，再让调度器选入足够多的请求，形成较大的批次，提高离线推理吞吐量。” 不要额外添加说明框标题。

Accuracy constraints: 模型权重也是显存的重要部分，不能把显存只分成 KV 与执行两部分。CUDA 图缓冲区可能持续占用显存，不能笼统称所有执行显存为临时内存。KV 空间影响并发容量，不表示它越大每个请求必然越快。缓存池使用率与缓存命中率必须区分。无需画跨请求共享一定发生或给出通用最佳比例。保持简洁，不添加未列出的解释性段落。
No title: 不要在图顶添加“GPU 显存使用”“显存调优”或任何其他标题；直接展示上述分区和图解。
