生成一张中文技术科普信息图，解释模型量化(Quantization)，并帮助初学者看懂 SGLang 的 Quantization 文档。必须是横向 4:3 画布，建议 2048×1536 或同等比例的高清分辨率，不能做成长竖图。整张图是一个精心排版的完整海报。

风格：白底，深蓝大标题，蓝色表示原始数值/模型，紫色表示量化过程，绿色表示量化后的数据和部署，橙色表示需要注意的成本。扁平矢量插画，细描边圆角卡片，简洁服务器/芯片/文件图标，浅色底纹，字体清晰可读，充足留白。不要机器人、花哨装饰、水印、品牌标志。重点文字大，代码使用等宽字体；所有中文准确，不乱码、不重复句子。
流程统一从左向右，箭头连接准确。读者懂基本的模型推理，但不了解量化。重要术语使用“中文术语(English Term)”形式。严格保持以下技术边界和给定文案，可调整换行，不能杜撰性能数据。

总体版式：顶部标题；第一排两张卡片（1 约占 55%，2 约占 45%）；第二排整宽卡片 3，内部为两条横向流程；第三排卡片 4 和 5 左右并排；底部一条醒目的收益与代价说明。内容完整但不要密集到像论文。为长的参数名留出足够宽度。

顶部：
大标题：“模型量化(Quantization)”
副标题：“用更少的位数表示模型数值：理解精度、显存和 SGLang 的使用方式”

卡片 1：“① 量化在做什么？”
主句：“把精细的数值，近似映射到更少的刻度。”
用非常直观的三步数值流程，配一小段刻度尺：
“原始数值 0.1234” → “INT8 存储值 12” → “近似还原 0.12”
第一条箭头标“缩放 + 舍入”，第二条箭头标“反量化(Dequantization)”。
下面一行清晰公式：“缩放因子(Scale) s = 0.01”
“round(0.1234 ÷ 0.01) = 12；12 × 0.01 = 0.12”
小注：“这是简化的 INT8 示例；FP8 / FP4 使用浮点格式。”
视觉上用近邻刻度显示 0.1234 被映射到 0.12，而不是数值变大或模型层数减少。

卡片 2：“② 位数与 W / A 怎么看？”
上半部三个水平长度条，表示相同数量权重的理论数据体积：
“16 bit：1” 全长蓝条
“8 bit：1/2” 半长紫条
“4 bit：1/4” 四分之一长绿条
清楚注释：“只比较权重数据；缩放因子等开销另计。”
下半部用两行对应关系：
“W = 权重(Weights)：模型学到的参数”
“A = 激活值(Activations)：计算中的中间数值”
突出示例：“W4A16 = 权重 4 位，激活值 16 位”
“W8A8 = 权重 8 位，激活值 8 位”
不要把 W/A 解释成输入/输出，也不要暗示量化改变 token 的数量。

卡片 3：“③ 文章的两条路线：离线量化与在线量化”
两条清晰的横向泳道，左侧分类标签，4 个节点之间用箭头连接，显示量化发生在哪个时间点。

第一条，蓝绿配色，标签“离线量化(Offline Quantization)”：
“原始权重” → “量化工具处理” → “保存已量化权重” → “SGLang 加载并推理”
“量化工具处理”节点下方小标签：“部分方法需要校准数据(Calibration Data)”
泳道下方说明：“部署前完成；量化结果可复用，启动时直接加载。”
旁边小绿色标记：“原文推荐优先考虑”

第二条，蓝紫配色，标签“在线量化(Online Quantization)”：
“原始权重” → “SGLang 加载 / 启动” → “在此完成权重量化” → “进入推理”
泳道下方说明：“使用方便；启动耗时和峰值显存可能增加。”
卡片底部一个重要小注：“在线权重量化通常在加载阶段完成；激活值可在推理时动态量化，取决于具体方法。”
不要画成每生成一个 token 就重新量化全部模型权重。不要把在线解释成联网或调用云 API。

卡片 4：“④ 文档里这些名字各负责什么？”
四行小表格或分层清晰的四个条目：
“数值格式：INT8、FP8、NVFP4 等”
“量化算法：AWQ、GPTQ 等”
“量化工具：AutoRound、ModelOpt、ModelSlim 等”
“计算内核(Kernel)：CUTLASS、Triton、AITER 等”
下面画一个小型横向关系：
“量化后的模型” → “兼容的计算内核” → “硬件执行”
最后一句：“格式、模型、硬件和内核需要匹配。”
不要把 AWQ 和 GPTQ 写成浮点数值格式，不要画未经证实的硬件支持矩阵。

卡片 5：“⑤ SGLang 的关键配置”
用三条目的紧凑配置卡，每个参数行清晰等宽、不得漏字符：
第一条标题：“加载已量化模型”
代码：“--model-path <已量化模型>”
说明：“常见格式可由模型配置识别。”
第二条标题：“原始模型在线转 FP8”
代码：“--model-path <原始模型>”
代码：“--quantization fp8”
第三条标题：“选择矩阵乘法后端”
代码：“--fp8-gemm-backend auto”
代码：“--fp4-gemm-backend auto”
说明：“按所用精度选择对应参数；通常先用 auto。”
脚注：“具体支持范围，以模型格式和硬件兼容性为准。”
不要把第三条的两个参数描绘成必须同时启用，不要虚构启动成功的日志。

底部整宽总结，分为绿色收益和橙色权衡两个并排区域，保持易读：
绿色标题：“希望获得”
“权重占用更小 · 数据搬运更少 · 合适的内核可加速”
橙色标题：“需要验证”
“可能损失精度 · 实际速度依赖硬件与负载”
底部最小但仍清晰的一句：
“总显存还包括键值缓存(KV Cache)、激活值等；量化后要测试效果与性能。”
不写“位数减半，速度翻倍”，不写“无损”或“任何模型都通用”，不添加固定加速倍数。

生成方式：内置 imagegen。
初稿生成后进行了以下局部修正，最终图采用修正版：
对参考图片做一个非常小的局部编辑，保留原图横向 4:3 比例、分辨率、所有其他布局、文字、颜色和图标。
只修改左上方“① 量化在做什么？”卡片左下角的刻度尺区域（大约 x=40..365，y=316..422；原图为 1448×1086）。
删除这个区域的整条刻度尺、两个圆点、虚线箭头以及它们的全部数字标签（0.10、0.11、0.12、0.13、0.14 等）；它们的位置不准确。
在腾出的同一位置放一个浅橙色圆角提示框，使用居中、清晰、足够大的深色文字，两行：
“量化误差”
“|0.1234 − 0.12| = 0.0034”
这只是对数值例子的补充说明。不可改动上面的“原始数值 0.1234 → INT8 存储值 12 → 近似还原 0.12”流程，不可改动右侧“缩放因子(Scale) s = 0.01”的公式框，不可修改其他 2、3、4、5 区域和底部内容。不要增加新的刻度尺，不要新增水印。
