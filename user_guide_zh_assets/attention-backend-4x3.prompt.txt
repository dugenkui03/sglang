生成方式：内置 imagegen。

Use case: infographic-diagram
Asset type: 中文 SGLang 学习文档的科普信息图。
Primary request: 为《注意力机制与注意力后端》生成一张准确、易读、适合初学 AI Infra 的中文科普图，覆盖概念、Qwen 配置示例、后端选择和代码链路。
Canvas: 横向 4:3，严格宽高比 4:3，高分辨率，清晰的简体中文与等宽代码字体。白色背景、细线分区、轻量圆角卡片、留白充足。克制的技术出版物风格，颜色区分角色，避免照片和装饰性机器人。所有主流程尽量从左向右。标题醒目，说明字足够大，所有内容留在画面内。
Title (verbatim): “模型注意力机制与 SGLang 注意力后端”
Subtitle (verbatim): “模型规定计算规则，框架选择兼容实现”

Layout: 标题下方是四个编号板块，由上到下阅读；每个板块内部是横向流程。各块布局疏密平衡，第二块可以最大。只印指定文字及必要连接词，不额外添加新技术结论。

Panel 1 heading: “① 谁负责什么？”
四张并列卡片，左到右箭头连接：
“模型结构(Model Architecture)” / “规定注意力的计算规则”
“SGLang 推理框架” / “识别架构、组织执行”
“注意力后端(Attention Backend)” / “选择兼容的计算实现”
“计算内核(Kernel)” / “在硬件上执行张量运算”
每张配一个小型线性图标：网络层、流程调度、代码模块、芯片。

Panel 2 heading: “② 看 config.json：以 Qwen3.8-27B 为例”
小标签：“语言部分：text_config”
上半行画模型中连续四层的注意力类型：
“线性注意力(GDN)” → “线性注意力(GDN)” → “线性注意力(GDN)” → “全注意力(GQA)”
在四个模块整体下方加括号：“重复 16 组：48 层线性注意力 + 16 层全注意力”
旁边两行配置：“num_hidden_layers = 64” / “full_attention_interval = 4”
小注释：“这里标注每层的注意力类型；每层还包含前馈网络等计算。”
下半行两张并列小卡：
左卡标题：“全注意力：分组查询注意力(GQA)”
内容：“24 个 Q 头，4 组 K/V，head_dim = 256”
一个明确的图示：6 个小方块 Q1、Q2、Q3、Q4、Q5、Q6，全部连到同一块“K/V”；下面注明“每 6 个 Q 头共享一组 K/V；共 4 组”
右卡标题：“线性注意力：门控 Delta 网络(GDN)”
内容：“更新循环状态，汇总历史信息”
小型流程：“历史状态 + 当前输入 → 更新后的状态”
参数两行：“键头 16，值头 48” / “键头与值头维度均为 128”
底部小条：“先看配置了解结构 → 再看模型代码了解计算”
不要将 GQA 说成与全注意力互斥的选项。这里的 Qwen3.8 检查点采用 qwen3_5 架构，不要擅自把标题中的模型名改成 Qwen3.5。无需展示所有 config 字段。

Panel 3 heading: “③ 相同计算规则，可以选择不同实现”
左右两条清晰的并行路由，避免交叉箭头：
“全注意力层” → “--attention-backend” → “FlashInfer / FlashAttention / Triton 等”
“GDN 线性注意力层” → “--linear-attn-backend” → “兼容的线性注意力内核”
下一行文字：“选择依据：模型机制 + 硬件 + KV 缓存格式 + 功能需求”
两个并列小标签：“预填充(Prefill)：处理输入” / “解码(Decode)：逐步生成”
旁注：“两个阶段可分别选择兼容后端”
准确表达是选择实现，不能画成修改模型的数学结构，也不能表示所有后端在所有硬件上通用。

Panel 4 heading: “④ 核心代码链路：即时执行(Eager Execution)”
小字：“以下展示标准注意力路径”
横向流程：“EagerRunner” → “初始化批次元数据” → “model.forward” → “RadixAttention” → “注意力后端” → “计算内核”
“初始化批次元数据”下标：“需要时 init_forward_metadata”
“注意力后端”下方两行：“Prefill → forward_extend” / “Decode → forward_decode”
让两行解释归属于注意力后端，不画成 Prefill 与 Decode 同时执行的顺序链。
Footer, verbatim: “注意力后端负责注意力计算；完整推理还包括调度、缓存管理、前馈网络与采样。”
Constraints: 科学准确、清晰中文、层次明确，字号可读，箭头无歧义；严格 4:3 横向；不添加公司 logo、水印、虚构性能数字、命令启动示例或额外大段文本。
