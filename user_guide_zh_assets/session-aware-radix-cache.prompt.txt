首次生成提示词：
Use case: infographic-diagram
Asset type: 中文技术学习文档中的科普图，横向 4:3，清晰高分辨率。
Primary request: 为 SGLang 的“会话感知基数缓存”画一张准确、容易理解的中文科普图，解释会话引用、软保护、缓存淘汰、关闭会话和三类组件。
Style/medium: 精致的工程科普信息图，白色背景、清晰深色中文无衬线字体、简洁的缓存方块和箭头。少量蓝绿表示仍被引用的缓存，灰色表示未引用，橙色表示空间压力和淘汰。信息分区，留白充足，中文文字准确，不用花哨装饰。所有主要流程从左到右。不要公司 logo、水印或虚构性能数字。
Composition: 顶部标题；第一行横向使用流程；中间主区域用一个全注意力缓存树示例配合淘汰顺序；下部三个组件卡片与简短级联说明；底部两条要点。文字足够大，不拥挤，不裁切。

准确排版以下内容，不额外添加大段文字：
标题：“会话感知基数缓存”
副标题：“Session-Aware Radix Cache｜内存紧张时，优先保留活跃会话引用的可复用缓存”
小标签：“UnifiedRadixCache” 和 “--enable-session-radix-cache”

第一行标题：“① 每轮传相同 ID，请求完成后登记引用”
三个从左到右连接的步骤框：
“每次请求\n完整提示词 + session_id”
“完成推理\n自动登记可复用缓存”
“下一轮请求\n匹配前缀，复用缓存”
配套小字：“session_id 仅标记缓存引用；上下文由每次请求提供；无需 /open_session”

中间标题：“② 软保护：先淘汰无引用缓存”
在这一节左侧画准确的全注意力示例：树从左向右生长，根节点到“共享前缀\n引用数 2”，再分叉成“会话 A 的前缀尾部\n引用数 1”和“会话 B 的前缀尾部\n引用数 1”；根节点另有一条灰色的独立分支“旧前缀\n引用数 0”。会话 A、B 用虚线连接各自叶节点，说明这是引用关系。不要把灰色旧前缀画成两条活跃会话前缀的一部分。
树下标注：“全注意力：引用从叶节点覆盖到根的可复用前缀路径”
在这一节右侧用醒目的紧凑流程：“空间不足 → 淘汰引用数 0 → 仍不足 → 淘汰有引用的缓存”
强调：“软保护 ≠ 锁定内存”
小字：“全注意力：有引用时，先淘汰引用较少的节点，再按配置策略排序。”
不要暗示有引用缓存永不淘汰，不要暗示关闭会话立即清空。

下部标题：“③ 三类组件，分别跟踪引用”
三张并列卡片，精确短文：
“全注意力 (Full Attention)\n保护对象：可复用前缀路径\n顺序：无引用 → 引用少 → 配置策略”
“滑动窗口注意力 (SWA)\n保护对象：窗口尾部 + 页对齐余量\n顺序：无引用 LRU → 有引用 LRU”
“Mamba\n保护对象：叶节点上的可复用状态\n顺序：无引用 LRU → 有引用 LRU”
卡片下面小字：“LRU：最近最少使用。设备端与主机端采用相同的会话优先保护原则。”
级联条：“内部节点：Full 淘汰也带走 SWA、Mamba；SWA 淘汰也带走 Mamba；Mamba 仅影响自身。叶节点淘汰移除全部组件。”

底部用关闭会话图标加从左到右箭头：
“/close_session → 移除该会话引用 → 缓存仍可复用，恢复正常淘汰顺序”
脚注：“应用会话结束时调用，包含出错与取消路径。”

Constraints: 科学准确，注意数据共享和软保护语义。全注意力的引用数量优先级不要套用到 SWA 或 Mamba；后两者是无引用、有引用两轮 LRU。保护仅针对可复用缓存的淘汰偏好。不要把 session_id 画成自动拼接聊天历史或内存锁。英文代码标识保持原样，图表重点突出而非代码截图。

最终背景修正提示词：
Use case: precise-object-edit
Edit target: 所提供的“会话感知基数缓存”科普图。
Change only: 将画布和所有背景改为完全不透明的纯白色背景，确保整张图 alpha 全部为 255，不要透明区域、黑色空洞、抠图边缘或背景噪点。输出一张正常可阅读的完整白底信息图。
Preserve: 完整保留现有 4:3 横向布局、全部中文与英文文字、节点、箭头、配色和图标。标题与正文保持清晰的深色文字，浅色卡片保持不透明。不得改变图中的技术含义，不得删除内容。
Quality: 适合嵌入 Markdown 文档，清晰文字，白色四周留边，不裁切。

最终副标题修正提示词：
Use case: precise-object-edit
仅修改图片顶部大标题下方的一行副标题。删除现有整行副标题（英文和中文都删除），改为这一句准确的中文：“优先保留活跃会话引用的缓存”。逐字照写，不要添加其他文字。
保持大标题“会话感知基数缓存”、三个分区、全部节点、全部箭头、所有其他正文与配置标签完全不变。保持横向 4:3、完全不透明的白底、原有布局和配色。只修正这行副标题，确保没有错别字。
